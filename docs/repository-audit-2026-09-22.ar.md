# مراجعة المستودع — NEXA @ 085a9c7 — 2026-09-22

> **⚠️ هذه نسخة إعادة بناء (استعادة)، ليست الأصل المجمد.**
> التقرير الأصلي كُتب في جلسة `arena/01a0cabd-nexa` وبقي untracked ولم يُدفع أبدًا إلى GitHub،
> وفقد مع بيئة تلك الجلسة. هذه النسخة مُعادة الاشتقاق بالكامل من الكود في الـ commit
> `085a9c7898c9b8f19b59f33a4a298d67730a8a53` بتاريخ 2026-09-22، بقراءة مباشرة للملفات
> (الملفات هي مصدر الحقيقة). كل ادعاء هنا مسنَد إلى `ملف:سطر` قابل للتحقق.
> الأرقام المجمدة تاريخيًا من الجلسة المفقودة منقولة كما هي في §2 مع بيان قابلية التحقق.

---

## 0. بطاقة الهوية

| البند | القيمة |
|:---|:---|
| المستودع | `elazamey/nexa` |
| الـ commit المُراجَع | `085a9c7898c9b8f19b59f33a4a298d67730a8a53` (main — "Grand Synthesis & 2026 Zero-Cost Edge RAG") |
| فرع العمل | `arena/01a0cb16-nexa` |
| التاريخ | 2026-09-22 |
| المنهج | قراءة مباشرة للكود + تشغيلة أساس واحدة موثقة + اشتقاق فجوات → تذاكر |
| نطاق التركيز | خط الأنابيب الأمني `src/security/**` (Agentic Bug Hunter) — المنطقة التي تشير إليها تذاكر D1.x |

## 1. سياق الاستعادة (لماذا هذا التقرير نسخة ثانية)

الجلسة السابقة أنتجت: تقرير مراجعة + ملفات أدلة + `Detector_Independence_Assertions.md` +
`Artifact_Reader_Contract.md` (v0.3) + `self-model/gaps.json` + اختبارات فجوات وحراس — **كلها كانت untracked ولم تُرتبط بأي commit**،
لذا لم تنتقل إلى هذه البيئة (تحقق: شجرة عمل نظيفة عند 085a9c7، لا stash، لا كائنات معلّقة
في git، لا فرع `01a0cabd` على GitHub، ولا أي ملف مطابق في نظام الملفات).
القرار المعتمَد من المستخدم: استئناف المسار بأدوات كاملة مع ضمان عدم الكسر،
وإعادة بناء الطبقة المفقودة من الكود نفسه **موسومةً كإعادة بناء** — لا ادعاء بأنها الأصل.

**الدرس المؤسسي:** أي عمل غير مرتبط بـ commit في نهاية الجلسة يُفقد. من الآن: كل إنجاز
يُثبَّت بـ commit ثم push على فرع الجلسة فورًا.

## 2. خط الأساس المُتحقق مقابل الأرقام المجمدة

| الرقم | القيمة | الحالة |
|:---|:---|:---|
| اختبارات المجموعة الكاملة | **545 / 545 نجاح / 0 فشل** (node v22.22.3، ‏32.0 ثانية) | ✅ مُتحقق اليوم بالقياس (`npm test`) |
| probes (ادعاء الجلسة المفقودة) | 13 | ⚠️ مجمّد تاريخيًا؛ **غير قابل للتحقق الحرفي** — تعذّر تحديد مصدر العدّ في المستودع الحالي بيقين، ولا يُدَّعى تحققه |
| التوقع 557 للتشغيل الكامل القادم | 557 | ⛔ **ملغى** — كان يشمل اختبارات الجلسة المفقودة؛ يُستبدل بقياس فعلي بعد كل تذكرة (يُسجَّل في `self-model/baseline.json`) |

الأثر الجانبي للتشغيل: تعديل `dashboard/data/hunt-memory.json` — **استُعيد من HEAD** بعد
القياس (قاعدة ثابتة). القياس موثق في `self-model/baseline.json`.

## 3. جرد نطاق المراجعة

- `packages/` — 17 حزمة (parser، lexer، ast، runtime، policy، evidence، crypto، identity، protocol، capability، cell(s)، cellular-evolution، learning، evolution، compiler، cli)
- `tools/` — 45 أداة (bughunter، celia-*، omega-*، google-*، vectors، permission-probe/proof، dashboards)
- `src/security/` — خط الأنابيب الأمني: `agentic-hunter.js` + `hunter/` (14 وحدة)
- `tests/` — 51 ملف اختبار (بما فيها `agentic-hunter.test.js` الذي يغطي خط الأنابيب)
- `docs/evidence/` — أدلة مجمدة تاريخيًا (45 ملفًا) — **لا تُمس** (يحميها guard بقائمة hashes)
- `dashboard/data/` — مخرجات حية (`bug-report.json`, `hunt-memory.json`) — تُعدَّل بالتشغيل وتُستعاد

**نطاق هذه المراجعة:** خط الأنابيب الأمني فقط (من Recon إلى الإيصال المشفّر)، لأنه موضوع
تذاكر D1.x. بقية المستودع يعمل بمجموعته الخضراء (545/545) ولا يُلمس في هذه التذاكر.

## 4. مصادر الفشل A01–A13 (كلها مُسندة إلى الكود الحالي)

| الرمز | مصدر الفشل | الإسناد |
|:---|:---|:---|
| **A01** | شهادة ذاتية لنتيجة البوابات: `certifyFinding` يكتب `gateScore: '7/7_PASSED'` و `verified: true` حرفيًا دون أي تحقق — الإيصال يدّعي ما لم يجرِ | `src/security/hunter/nexa-evidence-bridge.js:46-47` |
| **A02** | بوابة بقرار ثابت: `GATE_7_SAFE_TESTING_COMPLIANCE` تكتب `pass: true` حرفيًا — لا تُحسب أبدًا | `src/security/hunter/seven-gate-validator.js:56` |
| **A03** | `GATE_2_REPRODUCIBILITY` إرشادية ضعيفة: تمر بمجرد وجود locator نصي (`endpoint \|\| file \|\| parameter`) دون أي دليل إعادة إنتاج | `src/security/hunter/seven-gate-validator.js:31` |
| **A04** | لا يوجد «قارئ artifacts» إطلاقًا: لا مكوّن يقرأ دليل الإعادة إنتاج ويتحقق منه؛ `GATE_4_POC_EVIDENCE` يمر بوصف نصي + تصنيف فقط | `seven-gate-validator.js:40` (غياب كامل للمكوّن) |
| **A05** | اقتران الكواشف بالمنسِّق: ثلاثة كواشف inline داخل `_inspectFile` تدفع نتائجها إلى حالة المنسِّق `this.findings` | `src/security/agentic-hunter.js:81-116` |
| **A06** | منطق كشف أعمى للنطاق: شرط `UNHANDLED_ASYNC_ERROR` يُقيَّم على `content` الملف كاملًا لا نطاق الدالة → كشف حقيقي يُفقَد بسبب `try` في دالة أخرى | `src/security/agentic-hunter.js:81` (وكذلك :104) |
| **A07** | إيصالات غير حتمية: `findingId` من `Date.now()`، والـ digest يُحقن فيه `timestamp` لحظي عند غيابه → نفس الـ finding ينتج معرفًا وdigest مختلفين | `src/security/hunter/nexa-evidence-bridge.js:33,40` |
| **A08** | توقيع غير مربوط بالحمولة: `verifyReceipt` يتحقق من التوقيع على digest مُقدَّم دون إعادة حسابه من الـ finding → إيصال «صالح» مع حمولة مستبدلة | `src/security/hunter/nexa-evidence-bridge.js:59-73` |
| **A09** | انهيار بوابتين في مسند واحد: `GATE_3` و `GATE_6` يحملان نفس الشرط الدلالي (severity ∈ {CRITICAL, HIGH, MEDIUM}) — «الأسئلة السبعة» فعليًا خمسة مساند | `src/security/hunter/seven-gate-validator.js:36,55-57` |
| **A10** | خلط الأصناف: `signFinding` يوقّع أي payload عام (بيانات إصدار مثلًا) بنفس ختم `7/7_PASSED` | `src/security/hunter/nexa-evidence-bridge.js:52-55` |
| **A11** | ذاكرة fail-open: `HuntMemory._load` يبتلع فساد JSON بصمت (`catch` فارغ) ويُرجع ذاكرة فارغة؛ و`save()` يبتلع أخطاء الكتابة | `src/security/hunter/hunt-memory.js:20-23,37-40` |
| **A12** | ثقة بسياق المتصل في قرار النطاق: `autopilot` يمرر `{ inScope: true }` حرفيًا، و`GATE_1` يعتمد سياق المتصل لا مصدرًا مستقلًا (بيانات recon) | `src/security/hunter/autopilot.js:55` + `seven-gate-validator.js:26` |
| **A13** | ادعاءات تقرير بلا تحقق: `bug-report.json` يُصدَّر ومعه إيصالات تدّعي 7/7 دائمًا و`verified: true` تسويقية | `src/security/agentic-hunter.js:119-125` |

**ملاحظة منهجية:** `tests/agentic-hunter.test.js` (سطر ~150) يؤكد حاليًا السلوك المعيب
نفسه (توقيع finding بلا artifact/bypass مع ختم 7/7_PASSED) — أي أن السلوك المعيب
**مُختبر ومطلوب** في الفرع الحالي. هذا الاختبار سيُحدَّث ضمن تذكرة D1.2 تحديثًا موثقًا،
لا صمتًا.

## 5. فهرس الفجوات (يُدار في `self-model/gaps.json`)

| الفجوة | العنوان | المصادر | الحالة عند إعادة البناء |
|:---|:---|:---|:---|
| **D1.1** | طبقة منهجية الجلسة (تقرير + assertions + عقد + gaps + حراس) | — (منهجية) | **enforced** عند إعادة البناء (الحراس أنفسهم) |
| **D1.2** | Artifact Reader + GATE_2 (§5.1) + certifyFinding (§6.1/§6.3) — **أول تذكرة src/** | A01, A03, A04, A07, A08, A10, A13(الجوهر) | **أُغلقت 2026-09-22**: أحمر موثق (`self-model/evidence/d1.2-red.tap`) ثم أخضر (`d1.2-green.tap`)؛ enforced في `gaps.json` بتحقق حي `tests/artifact-reader.test.js`؛ القياس الكامل بعدها 584/584 |
| **D1.3** | GATE_7 بقرار ثابت | A02 | مفتوحة |
| **D1.4** | انهيار GATE_3/GATE_6 في مسند واحد | A09 | مفتوحة |
| **D1.5** | اشتقاق قرار النطاق (GATE_1) من مصدر مستقل | A12 | مفتوحة |
| **D1.6** | HuntMemory fail-closed | A11 | مفتوحة |
| **D1.7** | فصل الكواشف inline عن المنسِّق | A05, A06 | مفتوحة |
| **D1.8** | اتساق bug-report مع الإيصالات الفعلية | A13 (المتبقي) | مفتوحة — محجوبة بـ D1.2 |

ترتيب العمل: **D1.2 أولًا** (هي التذكرة المتفق عليها: تُغلق باختبار انعكاس في `tests/`
خارج known-gaps، ثم `enforced:true` + `verification` + `closed_at` في نفس التغيير،
بعد إثبات الفشل قبل الإصلاح). ما بعدها بترتيب الفجوات أعلاه.

## 6. المجمَّد والمحمي (لا يُمس)

1. `docs/evidence/**` — 45 ملفًا بقائمة hashes مجمّدة داخل `tests/guards/evidence-frozen.test.js`؛ أي تعديل/إضافة/حذف يكسر الـ guard عمدًا.
2. الأرقام المجمدة (§2): 545 (مُتحقق)، 13 probe (غير قابل للتحقق — يُنقل كما هو ولا يُدَّعى)، 557 (ملغى).
3. اختبارات المجموعة الكاملة تبقى خضراء؛ التحديثات الوحيدة المسموح بها هي الموثقة داخل تذكرة (مثل تحديث كتلة certifyFinding في D1.2).
4. `dashboard/data/hunt-memory.json` يُستعاد من HEAD بعد كل تشغيل كامل.

## 7. قواعد الجلسة (مُفعَّلة بالحراس، لا بالتذكر)

1. لا تعديل على كود المنتج قبل فتح التذكرة صراحةً (D1.2 مفتوحة في commit مستقل يسبق كود التنفيذ).
2. اختبارات `tests/known-gaps/` نجاحها = إعادة إنتاج الفشل — لا تُحسب دليل سلامة، وتُزال عند إغلاق فجوتها في نفس تغيير الإغلاق.
3. `enforced:true` بلا `verification` في `tests/` خارج known-gaps = مرفوض (يُختبر آليًا في `tests/guards/gaps-consistency.test.js`).
4. فحص الصياغة السريع: `node --test tests/guards/*.test.js tests/known-gaps/*.test.js` (لا يُمرَّر مجلد مباشرة إلى `--test` على Node 22).
5. التشغيل الكامل فقط عند الحاجة، وبعده استعادة `hunt-memory.json` من HEAD، وتسجيل الرقم في `self-model/baseline.json`.

---
*نُقل هذا التقرير إلى الفرع `arena/01a0cb16-nexa` ضمن commit إعادة البناء. المراجع التعاقدية: `docs/Artifact_Reader_Contract.md` (v0.3-rebuild)، `docs/Detector_Independence_Assertions.md` (17 assertion).*

---

## ملحق O — نتائج حدود التشغيل (إضافة 2026-09-22، فرع `arena/01a0cb5b-nexa`)

> **طبيعة هذا الملحق:** إضافة (append-only) — لا تعديل على §§0–7 أعلاه ولا على
> الأرقام المجمدة. يوثق نتائج مراجعة الحدود التشغيلية (خارج خط الأنابيب الأمني):
> الخادم HTTP، عزل المخرجات، المزامنة التوثيقية، الـ CI. كل نتيجة مسندة إلى
> `ملف:سطر`، ومسجلة في `self-model/gaps.json` كتذاكر D1.9–D1.16 بعائلة مصادر
> جديدة `O01..O08` (الحرف O = operational، لتمييزها عن عائلة A الخاصة بخط الصيد).

### O1. سجل القرار: حالة النشر على Render (سؤال P0-3 الأول)

* `render.yaml` موجود (blueprint باسم `nexa-dashboard`، `autoDeploy: true`)، لكن
  **لا يوجد أي مرجع لمنشور فعلي** في المستودع (لا `onrender.com`، لا tags، لا URLs).
* فحص حي بتاريخ 2026-09-22 للـ URL الافتراضي `https://nexa-dashboard.onrender.com/`
  و`/api/celia/state` أعاد نص `Not Found` — وهو **ليس** رد خادم NEXA (الذي يخدم
  HTML في `/` وJSON في `/api/celia/state`)، بل رد موجّه المنصة لخدمة غير موجودة.
* **القرار:** الحالة = **غير منشور** على الـ URL الافتراضي. لا طوارئ نشر؛ الترتيب
  المعتمد (P0-A → P0-B → P0-C…) يبقى كما هو. إن رُبط الريبو بـ Render لاحقًا،
  فـ `autoDeploy: true` سينشر تلقائيًا — لذا تبقى P0-B مبكرة احترازيًا.

### O2. خريطة التغطية بالمصرّحات (سؤال P0-3 الثاني — من الكود مباشرة)

| المسار | الحماية الفعلية | الحكم |
|:---|:---|:---|
| `POST /api/v1/workspace/write` | `authorizeWorkspaceWrite` (توقيع/قدرة/سياسة، رفض افتراضي) `tools/celia-dashboard-server.mjs:1177` | ✅ مقفل |
| `POST /api/v1/workspace/commit` | `commitWorkspace` (هوية→قدرة→سياسة→منفذ) `:1216` | ✅ مقفل |
| `POST /api/v1/terminal/execute` | `approvalLedger.consume` — لكن الـ approval نفسه مفتوح (أدناه) | ⚠️ مفتوح عبر سلسلة |
| `POST /api/v1/authorizations/request` | لا شيء — يُسجَّل الطلب باسم `dashboardOperator.kid` لأي متصل `:1362` | ❌ مفتوح |
| `POST /api/v1/authorizations/{id}/approve` | `approverKid ?? dashboardOperator.kid` + فحص سلسلة نصية بلا توقيع (`packages/policy/src/approval.js:399-402`) | ❌ مفتوح — **لا يوجد `CELIA_TERMINAL_AUTH` أصلًا** |
| `POST /api/v1/workspace/rollback` | لا شيء سوى `workspaceId` `:1245` | ❌ مفتوح (تغيير حالة) |
| `POST /api/v1/workspace/create` | لا شيء سوى `taskId` `:1131` | ❌ مفتوح (إنشاء) |
| `POST /api/v1/missions/create` + `/run` | لا شيء `:1582` | ❌ مفتوح |

سلسلة التجاوز عن بُعد (بلا أي سر): `request` → `approve {}` (يُقبل كهوية المشغّل
افتراضيًا) → `terminal/execute` (تنفيذ حقيقي داخل الـ jail). التخفيفات الباقية
(jail + قائمة برامج + ربط الهدف) دفاع أخير، لا حدّ مصادقة.

**علة عقدية ملازمة (اكتُشفت أثناء كتابة مُعيد D1.10):** مسار
`authorizations/{id}/approve|deny|consume` يستخرج المعرف بـ `split('/')` خامًا
بلا `decodeURIComponent`، بينما الواجهة (`NexaDashboard.jsx:371,386,400`) ترمّز
المعرف دائمًا — فنداء الواجهة الحقيقي يفشل 400. لا اختبار HTTP يغطي `approve`
أصلًا. تُعالج ضمن إغلاق D1.10 (فك الترميز + تثبيت العقد raw/encoded باختبار).

### O3. سجل عملاء الخادم (سؤال P0-3 الثالث + بوابة ما قبل الـ strangler)

| العميل | النوع | الدليل |
|:---|:---|:---|
| Dashboard SPA | `fetch` نسبي (~30 موقعًا) | `dashboard/src/**/*.jsx` |
| اختبارات HTTP | تنسخ السيرفر إلى tmpdir وتشغّله كابن (env لا يرث الأسرار) | `tests/celia-workspace-auth-helpers.mjs` (`PORT: '0'`) |
| لا عملاء آخرون | `bughunter.mjs` و`autopilot.js` لا يخاطبان الخادم عبر HTTP إطلاقًا (لا `fetch` ولا `http.request` إليه) | بحث شامل في `src/` و`tools/` |

أي تغيير على المحيط (مصادقة/حد معدل) يجب أن يبقى مفتوحًا عند غياب الـ env حتى
لا ينكسر العميلان أعلاه — وهذا قيد تصميم P0-B، لا خيار.

### O4. مصادر التشغيل O01–O08

| الرمز | مصدر التشغيل | الإسناد |
|:---|:---|:---|
| **O01** | ملفات tracked تُعدَّل من التشغيل: `HuntMemory` الافتراضي + `bug-report.json` المتصلب + سكربت اختبار بلا عزل | `src/security/hunter/hunt-memory.js:9`، `src/security/agentic-hunter.js:143-149`، `package.json` |
| **O02** | محيط HTTP بلا مصادقة عامة: سلسلة request→approve→execute مفتوحة + rollback/create/missions بلا مصرّح | `tools/celia-dashboard-server.mjs:1131,1245,1357,1431,1582` + `packages/policy/src/approval.js:399-402` |
| **O03** | الإنتاج يعمل مع `mock://memory` بصمت بلا حارس بدء | `tools/celia-dashboard-server.mjs:104-105` |
| **O04** | ادعاءات أعداد متقادمة في ثلاثة أماكن (501 مقابل 545 مقابل 314) | `README.md:66,504,512`، `pub-verifier.sh:29`، `publish-v0.1.plan.json` |
| **O05** | متغيرات env بلا مرجع مركزي ولا `.env.example` | `CELIA_*`/`SUPABASE_*`/`PORT` مبعثرة في `tools/` |
| **O06** | توثيق بلا فهرس/مالك (30+ ملفًا) | غياب `docs/README.md` |
| **O07** | ازدواج ملفات بلا حارس مزامنة (7 نسخ edge-rag متطابقة + workspace.html مكرر + index.html متباينان) | `adapters/edge-rag/` مقابل `dashboard/public/` |
| **O08** | بناء الداشبورد غير مُختبر في PRs | `.github/workflows/ci.yml` بلا خطوة dashboard (فقط `deploy-pages.yml` على main) |

### O5. التذاكر التشغيلية (تُدار في `self-model/gaps.json`)

| الفجوة | المسار | الأولوية | المصدر | الحالة |
|:---|:---|:---|:---|:---|
| **D1.9** | P0-A عزل مخرجات الاختبار (bootstrap إلزامي) | high | O01 | مفتوحة |
| **D1.10** | P0-B مصادقة محيط + حد معدل (env-gated) | critical | O02 | مفتوحة |
| **D1.11** | P0-C حارس بدء الإنتاج (fail-fast مع mock) | high | O03 | مفتوحة |
| **D1.12** | P1-A مزامنة أعداد الاختبارات | medium | O04 | مفتوحة |
| **D1.13** | P1-B ملف `.env.example` | low | O05 | مفتوحة |
| **D1.14** | P1-C فهرس `docs/` | low | O06 | مفتوحة |
| **D1.15** | P1-D حارس الملفات المكررة | medium | O07 | مفتوحة |
| **D1.16** | P2 بناء الداشبورد في CI | medium | O08 | مفتوحة |

قواعد ملزمة لهذه التذاكر (نفس نمط D1.x): إغلاق = اختبار انعكاس في `tests/`
خارج known-gaps + `enforced:true` + `verification` + `closed_at` في التغيير نفسه؛
مُعيد الإنتاج يُزال في تغيير الإغلاق نفسه؛ أي حارس يُعدَّل يُوثَّق سبب تعديله هنا.

---

### O6. سجل إغلاق D1.10 (P0-B) — أربع طبقات على حدود الخادم (2026-09-23)

جدول O5 أعلاه **لقطة تسجيل** لا لوحة حالة؛ الفهرس الحي هو `self-model/gaps.json`.
لذلك لم تُعدَّل خلايا «الحالة» في O5 (ملحق append-only، و§§0–7 خارج النطاق)،
ولا يُعَدّ ذلك فجوة موثقة معلّقة: كل صف يُقارن بـ `ticket_status` في الفهرس.

**ما أُغلق** — سلسلة O02 كانت مفتوحة من طرفين في آن: لا هوية على النقل، ولا قرار
موقَّع في الموافقة. الفاصل بينهما هو ما منع الخلط:

| # | الطبقة | التنفيذ | الاختبار |
|:--|:--|:--|:--|
| 1 | هوية المحيط + جلسة SPA + CSRF | `tools/celia-perimeter-auth.mjs` | `tests/server-perimeter.test.js` (9) |
| 2 | حد معدل (نافذة ثابتة) | `tools/celia-rate-limit.mjs` | `tests/server-rate-limit.test.js` (7) |
| 3 | fail-fast إنتاجي + عقد Render | `tools/celia-startup-guard.mjs` + `render.yaml` | `tests/server-production-failfast.test.js` (6) |
| 4 | عقد قرار الموافقة | `tools/celia-approval-http.mjs` + `tools/celia-operator.mjs` | `tests/server-approval-contract.test.js` (6) |

**القرارات الحدودية الستة** (المسجَّلة هنا لأنها ما يجعل الإغلاق قابلًا للمراجعة):

1. **حدود الجلسة**: `login → session id → Set-Cookie → middleware → req.nexaIdentity`.
   الكوكي `HttpOnly; SameSite=Strict; Path=/` و`Secure` تحت `NODE_ENV=production`
   أو `x-forwarded-proto=https`. لا سر قابل للوصول من JavaScript: المفتاح يُرسَل
   مرة واحدة فقط، ولا استجابة (ولا HTML) تعيده. الجلسات مخزَّنة كبصمات sha256 بسقف
   و TTL، لا كرموز خام.
2. **CSRF**: `HttpOnly` ليس دفاع CSRF. كل `POST/PUT/PATCH/DELETE` بمصادقة كوكي
   يستلزم إعادة رمـز `x-nexa-csrf` (double-submit؛ الرمز في كوكي مقروء للصفحة).
   عميل الترويسة (المفتاح) معفى من فحص CSRF وحده، لا من الجدار. الواجهة تركّب
   الرفيق في موضع واحد بدل 18 موضع استدعاء حتى لا يُنسى في أحدها.
3. **حدود مفتاح المشغّل**: `Authorization: Bearer` أو `X-Nexa-Api-Key` فقط؛
   المفتاح في query string مرفوض أصلًا (لا referer/سجل/history)، والمقارنة
   sha256 ثم `timingSafeEqual` (زمن ثابت، لا وحي طول). لا `.env.example` هنا
   (تذكرة D1.13)، ولا المفتاح في حزمة أمامية أو إعدادها.
4. **حدود التفويض**: الترتيب تعاقدي —
   `Authentication → Rate Limit → Authorization/CSRF → Policy → Approval → Execution`.
   `authenticated ≠ authorized` و`approved ≠ authenticated`: لا مسار يقرأ وجود
   الهوية كتفويض، ولا توقيع يشتري هوية. الدفتر (`packages/policy`) يبقى جذر ثقة
   الموافقة؛ الطبقات الأربع لا تقرر شيئًا بدلًا منه.
5. **عقد الموافقة**: `approve`/`deny` يستلزمان `approverKid` مصرَّحًا و`scope`
   مصرَّحًا وتوقيعًا على `nexa:approval-decision:v1\n` + `canonicalBytes` لكل
   الحقول؛ `consume` يفك الترميز فقط؛ `/sign` يوقّع ولا يمس الدفتر. الانعكاس
   مُثبَّت: `approve {}` = 400 `NEXA_E_SCHEMA`، توقيع مفقود/موسَّع = 400
   `NEXA_E_SIG`، kid أجنبي بتوقيع صحيح = 400 `NEXA_E_UNTRUSTED`، معرف مرمَّز صالح
   = 200، ترميز مشوّه = 400.
6. **الحد كدفاع لا كتفويض**: 429 مع `Retry-After` بعد المصادقة وقبل التنفيذ، مفتاح
   الدلو قابل للضبط (`identity|ip|forwarded`) لأن عنوان الـ peer خلف proxy إجابة
   خاطئة في الاتجاهين؛ `/healthz` والمسارات الساكنة خارج الحد (probe مقفول =
   إعادة تدوير خدمة سليمة). لا اعتمادية جديدة، ولا اعتماد عليه لتأمين
   `terminal/execute` — واختبار يثبّت أن الرفض تحت الميزانية يأتي من العقد لا من
   الحد.

**بوابة الإغلاق** (نجاح 622/622 وحده لا يُغلق شيئًا):

- `npm test` 622/622، `npm run verify` أخضر، `git status` نظيف بلا استعادة يدوية.
- أحمر قبل الإصلاح ثم أخضر بعده: `self-model/evidence/d1.10-red.tap`
  (5/6 فشل — `approve {}` يعيد 200، `/sign` غير موجود، و`NEXA_E_APPROVAL_MISSING`
  بدل فك الترميز) مقابل `d1.10-green.tap` (6/6). الاختبار الوحيد الأخضر في
  الأحمر هو اختبار الوحدة للنقي الجديد — طبيعي: الحارس كان غائبًا.
- إزالة كل حاجز تُثبت فعاليتها: تعطيل الجدار ⇒ 4 من 9 حمراء؛ تعطيل الحد ⇒ 4 من 7؛
  تعطيل الحارس ⇒ 2 من 6. لا اختبار «زخرف».
- `production` بلا مفتاح (أو فارغ/مسافات) = exit 1 قبل `listen`؛ بمفتاح خاطئ/مفقود
  = 401؛ الإغراق = 429؛ `request → approve {} → terminal/execute` = **قرار DENY**
  مسجَّل في `/api/v1/timeline` كـ `AUTHORIZATION_RESULT`، لا 401 مخبَّأ خلف الجدار.
- بلا مفتاح (local/dev/test): كل اختبارات HTTP السابقة خضراء بلا تعديل، ومُعيد
  D1.11 بقي أخضر المتوقَّع (إنتاج + mock يقلع ويخدم) بعد تمرير مفتاح تجريبي له.
- النواة سليمة: لا سطر تغيَّر في `packages/{protocol,capability,policy,crypto,
  evidence}` ولا في الـ vectors؛ عقد الموافقة أُصلح في السيرفر. الإصلاحات أربع
  commits منفصلة قابلة للعكس، وكل طبقة «hook مضاف» لا تفكيك لـ
  `celia-dashboard-server.mjs`.

**ما لم يُفعَل عمدًا**: تفكيك `celia-dashboard-server.mjs` وتعريف الـ API العام
يبقيان بوابةً قبل Phase 1 (انظر O3)؛ `creative/approve` خارج النطاق؛ سياسة أصل
CORS لم تُمَس (`*` + same-origin عبر البروكسي، والكوكيز المقرونة بـ`*` ترفضها
المتصفحات أصلًا)؛ حد حجم الجسم لمواضع `req.on('data')` القديمة والـ fingerprint
المتاح في `/api/v1/authorizations/stats` مُسجَّلان كمتابعة في `gaps.json` تحت
D1.10 (`follow_ups`)؛ ودوران `NEXA_OPERATOR_SEED` تحذير عند الإقلاع لا فشل —
لأن البذرة المعلنة تجعل kid قابلًا للاشتقاق لأي مستنسخ، وهي مسؤولية المشغّل لا
هذه التذكرة.

---

### O7. سجل إغلاق D1.11 (P0-C) — منع ادعاء الاستمرارية الكاذب (2026-09-23)

**العقد المُغلق** ليس «mock://memory غير مرغوب»، بل:

```text
NODE_ENV=production ∧ NEXA persistence = mock://memory ∧ startup
  → NON-ZERO EXIT  ∧  NO LISTEN  ∧  رمز محدد (NEXA_E_PERSISTENCE_MOCK)
```

بالمقابل: `development + mock` يقلع كما كان حرفيًا، و`production + خلفية حقيقية`
يحفظ العقد ويقلع بلا أي علامة.

**الحارس يقرر من حكم البورت، لا من شكل الإعداد.** في هذه الشجرة
`tools/celia-vector-port.mjs:272-281` يُرجع `{createClient:null}` دائمًا، فأي
`SUPABASE_URL` «حقيقي» يسقط بصمت إلى mock. حارسٌ يفحص النص كان سيُصدق نشرًا
يكتب كل شيء في عملية ستُقتل عند أول إعادة تشغيل — أي انه يغطي الادعاء بدل أن
يمنعه. لذلك الترتيب: ما يقرره البورت (`_isMock`) حجة، وما ادعاه الإعداد يُستخدم
لتسمية السبب فقط (`unconfigured | mock-url | no-driver`)، وما يعلنه الإنتاج قرار
حتمي.

**لا listen — مُثبَت بطريقتين مستقلتين** (لأن «خرج بعد أن خدم» لا يشفي شيئًا):
غياب رسالة `{type:'ready'}` من ناقل الإقلاع (تُرسل من داخل `listen()` نفسها)،
واستطراق TCP متواصل لمنفذ محجوز مسبقًا طوال عمر العملية: صفر اتصال ناجح، ثم
قابلية ربط نفس المنفذ بعد الخروج. الاختبار الأحمر أثبت العكس قبل الإصلاح:
`listened: true` و«المنفذ قُبل أثناء حياة العملية».

**المخرج الوحيد إقرار صريح بحرفه**: `NEXA_PRODUCTION_PERSISTENCE=ack-mock-ephemeral`
— وليس مفتاح إيقاف:
- حارس المحيط (D1.10): تقديم مسارات مُغيِّرة بلا مصادقة *غير آمن*، ولا جملة مشغّل
  تجعله آمنًا ⇒ لا override إطلاقًا.
- حارس الاستمرارية (D1.11): التشغيل بحالة مؤقتة *ليس ادعاءً* ما دام أحد لا يقول
  «دائم» ⇒ الإقرار مسموح، لكنه مطبوع تحذيرًا عند الإقلاع، و`/api/v1/system/status`
  يظل يصرّح `memory: DEMO`، وأي قيمة أخرى (`1`، `yes`، فارغة، بأحرف كبيرة، أو
  بمسافة ذيل) ليست إقرارًا — غياب القرار ليس قرارًا، نفس البديهية التي أغلقت
  `approve({})`.

**ما لم يُمَس عمدًا** (نطاق التذكرة حرفيًا): لا Supabase refactor، لا تنفيذ تخزين
دائم، لا Render migration (عقد `render.yaml` وُثِّق وأُضيفت متغيرات الاستمرارية
`sync: false` دون أي سر في git)، لا تفكيك لـ`celia-dashboard-server.mjs`، ولا سطر
في النواة. مُعيد known-gaps حُذف في نفس التغيير بعد أن صار مضمونه معكوسًا
(«إنتاج + mock يقلع»)؛ ومنطوق الحارس يبدأ بالرمز قبل السرد ليكون قابلًا للتفتيش
في CI (`[nexa] NEXA_E_…: startup refused`).

**متابعات مسجلة في `gaps.json` تحت D1.11** (لا تُغلق بالخطأ): `CELIA_COMMIT_STATE_DIR`
قد يشير إلى مسار مؤقت في الإنتاج — نفس فئة الادعاء؛ و`NexaGovernedMemoryEngine`
والمهمات وأحداث event-sourcing في الذاكرة وحدها، فإما خلفية دائمة أو لافتة DEMO
مطردة.

**بوابة الإغلاق**: `npm test` أخضر، `npm run verify` أخضر، `git diff --check` نظيف،
`git status` نظيف؛ أحمر→أخضر في `self-model/evidence/d1.11-red.tap` (7 فشل من 8)
مقابل `d1.11-green.tap` (8/8)؛ وتعطيل الحارس وحده يقلب اختباري الإقلاع (3 و4)
إلى أحمر.

### O8. سجل إغلاق D1.12 (P1-A / O04) — الأعداد تُقاس، ولا تُروى (2026-09-23)

**ما ظنّه التقرير «أرقامًا نسيها أحد» تبيّن، عند القراءة من الكود الحالي، أنه ختمٌ بعدد
لم يُقرأ.** النص القديم ذكر ثلاثة أرقام متقادمة (`README.md:66,504,512` مقابل
`pub-verifier.sh:29` مقابل `publish-v0.1.plan.json`)؛ الفحص الحالي أظهر أن الأرقام كانت
العَرَض، وأن الداء في السطر نفسه — وفي رسالة النجاح التي تليه:

```bash
if npm test 2>&1 | tail -30 | grep -q "314" || npm test 2>&1 | grep -q "pass"; then
```

بوابة النشر تُطبِق `set -o pipefail`، فمقياسها الحقيقي للون لم يكن العدد بل رمز خروج
`npm test`؛ أما العدد المطبوع فهو نصّ ثابت: مع مجموعة مُصطنعة من اختبارين أخضر (2/2)
طبع الفحص القديم `✅ tests_green: 314/314` ومضى — ختمٌ بعدد لم يُقرأ أصلًا، ولا ذكر لـ
`# fail` ولا أي أرضية تمنع انكماش المجموعة أو استبدالها. الفحص الجديد يقرأ
`# tests/# pass/# fail` ويرفض: `❌ suite shrank below the recorded floor: 2 < 633`.
و`tools/check-metrics.mjs` — السنَد الحقيقي الوحيد — كان خارج هذا المحقق، فبقي «501/501»
في الوثائق حرفيًا لا يربطه بالقياس شيء. القول إن الحراسة موجودة لأن `pub-verifier.sh`
«يفحص الاختبارات» كان خطأً بنيويًا، لا إهمالًا في التحديث.

**العقد المُغلق**:

```text
عددٌ في وثيقة أو في بوابة نشر  ⇔  يُقرأ من قياس، أو يُرفض
  pub-verifier [2/5]: تشغيل واحد → # tests / # pass / # fail
      fail > 0            → REFUSE
      pass ≠ tests        → REFUSE  (TAP غير متسق)
      tests < floor       → REFUSE  (الأرضية = self-model/baseline.json → live_measurement)
  publish plan: record_status يذكر timestamp الخطة  → وإلا REFUSE
  README/SECURITY: لا «N PASS / M FAIL of K»، لا «Latest verification: <رقم>»،
                   وكتلة NEXA_METRICS = live_measurement (وفقط هناك)
```

**سجل واحد، باتجاهَي الفحص.** `self-model/baseline.json` صار يحمل قياسين متمايزين:
`measured` (قياس التقرير المجمّد عند `085a9c7`) و`live_measurement` (القياس الجاري،
بتاريخه ولحظته وأداته). الوثائق تُحال إلى الثاني؛ والأول يبقى كما هو لأن التقرير المجمّد
يجب أن يظل قابلًا للتكذيب. حارس `tests/guards/docs-consistency.test.js` كان يثبّت `545`
حرفيًا في `assert.equal` — أي أن كل إعادة قياس كانت تُكسر بدل أن تُصحَّح — فصار يقرأ رقم
التقرير من نص التقرير نفسه ويقارنه بالسجل.

**توسيع معلن في نفس النطاق**: `tools/check-metrics.mjs` صار يقيس `Ω error codes` و
`Gated namespaces` و`Attack categories` أيضًا. السبب أن README كان يعلن «72 Ω error codes»
بينما `npm run posture` يطبع 86 منذ إغلاق D1.10 — أي أن عددًا آخر كان حرفيًا بلا سَنَد؛
تركه كان سيعني إغلاق التذكرة مع إبقاء نفس الفئة من الكذب في نفس الملف.

**في نفس السطر، بوابة البوابات.** `[1/5] gates_closed` كان `grep -q "6 gates CLOSED"` — عدد
حرفي آخر داخل قرار نشر: تُرفض ستة أبواب مفتوحة لأن النص يقول «7»؟ لا: تُحكم البوابة بصيغة سطر
يطبعه الفاحص. صارت تسأل `tools/check-posture.mjs` نفسه (رمز الخروج هو صاحب القاعدة) وتطبع العدد
الذي قاسه من `NEXA_METRIC closed_gates`. الدليل مُسجَّل في `d1.12-gate-bypass.txt`: الفحص القديم
يمنح `✅ tests_green: 314/314` لمجموعة من اختبارين (2/2)، والفحص الجديد يرفض
`suite shrank below the recorded floor: 2 < 633`، وعلى حمولة حمراء (`# fail 2` مع `# pass 98`)
يرفض `2 failing test(s) of 100`. لا استنتاج من نص: القياس جرى بـ`npm` وهمي على `PATH`.

**الأدلة** (`self-model/evidence/`): `d1.12-red.tap` — الحارس الجديد `tests/count-claims-sync.test.js`
على شجرة ما قبل الإصلاح (5 من 6 حمراء؛ والاختبار السادس يحمي الآلية القائمة فلا ينجبه الإصلاح)،
مع مُعيد known-gaps أخضر 5/5 (أي أن الفشل مُعاد إنتاجه قبل أي تغيير)؛
`d1.12-metrics-red.txt` — `npm run metrics`: «Measured: 632, Documented: 545» في الوثيقتين،
`exit 1`. ثم `d1.12-green.tap` (6/6) و`d1.12-metrics-green.txt` (docs match measured reality،
`exit 0`)، والمجموعة 633/633 (629 − 5 حُذفت + 6 أُضيفت).

**اختبار الانقلاب** (كل تغيير وحده يُحمرّ اختبارًا واحدًا فقط، ثم 6/6 بالاستعادة):
إعادة «501 PASS / 0 FAIL of 501» إلى README ⇒ اختبار الوثائق؛ تحريك `Total tests` في
README دون SECURITY.md ⇒ اختبار التطابق مع السجل؛ نزع `record_status` من خطة v0.1 ⇒
اختبار الخطط؛ شلّ شرط `-ne 0` ⇒ لا شيء (الدفاع متراكم: `pass ≠ tests` والأرضية تمسكانه)،
لذلك قِيست البوابة نفسها بمُخرَج مُصطنع: الفحص القديم يمرّ على 2/2 مختومًا «314/314»،
والجديد يرفض «below the recorded floor: 2 < 633». لا شيء من هذا يحتاج تشغيل المجموعة كاملة.

**ما لم يُمَس عمدًا**: لا rewrite للخطط الموقّعة (لا تصحيح لأرقامها التاريخية — الخطة سجل
موقّع بتاريخه، والوسم يحوّلها من ادعاء راهنية إلى سجل)، لا إزالة لـ`_padding` (يغيّر بايتات
ملف موقّع ولا يُثبِت شيئًا)، لا نقل لـ`npm run metrics` إلى داخل `npm run verify` (يعني تشغيل
المجموعة ثلاث مرات لكل بوابة؛ البوابة الحقيقية CI، و`count-claims-sync` هو الحارس السريع).
النواة (`packages/`, `spec/`) لم تُمَس بسطر؛ التغيير كله وثائق وأدوات وحارس.

**بوابة الإغلاق**: `npm test` 633/633، `npm run metrics` أخضر بـ`exit 0`، `npm run verify`
أخضر، `git diff --check` نظيف، `git status --short` نظيف بعد Commit، وفجوة واحدة مغلقة
بـ`enforced:true` و`verification: tests/count-claims-sync.test.js` وحذف مُعيدها في نفس التغيير.

### O9. سجل إغلاق D1.3 (P0-D / A02 / DI-07) — البوابة السابعة صارت تُجاب (2026-09-23)

**الداء كما ظهر في الكود الحالي، لا كما صيغ في التقرير.** السطر 64 من
`seven-gate-validator.js` يكتب `pass: true` للبوابـة «هل أُجري التحقق بغير إضرار وبلا قطع
خدمة؟». ما لم يقله النص: الضرر يتجاوز finding واحدًا — لأن `NexaEvidenceBridge.certifyFinding`
يُعيد التقييم **بنفسه** (§6.1) ثم يشترط 7/7 (§10.10). بوابة مُهداة واحدة كانت إذن تُغلّق
قاعدة الشهادة كلها: القارئ المستقل يجتاز، والسادسة تُحكم بالشدة، والسابعة لا تُسأل.

**العقد**:

```text
GATE_7 pass ⇔ ∃ سجلّ { nonDestructive:true, noServiceDisruption:true,
                       method:<معرّف>, attestedBy == artifact.producedBy (نسخة القارئ المنقّحة) }
                   ∧ لا إقرار بالضرر (serviceDisrupted|destructiveAction|causedOutage)
الغياب/النصّ/شهادة عامة واحدة لكل الكواشف → رفض مُعلَّل (§10.9)، لا تمرير
```

ثلاث قرارات تصميمية مُوثَّقة هنا:
1. **الربط لا الوجود**: `attestedBy` يجب أن يساوي منتِج الدليل بعد تنقيح القارئ — وإلا صارت
   شهادة واحدة في السياق تغطي كل finding، وهو «حكم مرفق» بلبوس دليل. لا يُمسّ عقد الـ artifact:
   §3.3 يمنع حقول الحكم الذاتي داخل artifact، فالسجل على الـ finding والقارئ يبقى حكمه الوحيد
   على الدليل (DI-05 لم يُلْمَس).
2. **لا حقول لحظية**: السجل يدخل الحمولة القانونية الموقّعة؛ `observedAt` كان سيكسر حتمية
   §6.3 التي يحميها D1.2. الصدق في الربط لا في الختم الزمني.
3. **الرفض يسمّي**: `failedGates[].reason` ورسالة الجسر صارت تُعدّد البوابات الساقطة وأسبابها؛
   «لم تكتمل 7/7» بلا سبب ليس تعليلًا.

**المُنتِجون يوقّعون على سلوكهم حيث هو معروف**: `VulnEngine.scanSurface` يرفق السجل عند مواضع
إنتاج الـ artifacts الثلاثة (`method: 'surface-model-analysis'` — لا يمرّر حركة شبكة أصلًا).
ومن لا يملك سجلًا يُرفض: الكواشف inline في `agentic-hunter.js` ما زالت تُنتج findings بلا
artifact، فترفضها GATE_2 وترفضها GATE_7 — وهذا أمانة لا عطلًا؛ تُزوَّد بدليلها وشهادتها في D1.7.

**القوالب قُوِّيت لا Assertions لِينت**: قالب D1.2 في `tests/artifact-reader.test.js` (وبناه
المعاد ترتيبه) وقالبا `tests/agentic-hunter.test.js` اكتسبا سجلًا صالحًا مربوطًا؛ لا assert
حُذف أو خُفِّف. الحتمية بقيت مفحوصة: نفس finding → نفس `findingDigest` قبل وبعد.

**العزل (الخاصية التي تطلبها التذاكر فردًا فردًا)**: إرجاع `pass: true` وحده في المُقيِّم —
بقي كل شيء آخر مُصلحة — يُحمرّ `tests/gates-computed.test.js` وحده (5 من 6) ويبقي
`tests/artifact-reader.test.js` 16/16، `tests/agentic-hunter.test.js` 10/10، ومُعيدات
D1.4/D1.5/D1.6/D1.7 خضراء. الدليل: `self-model/evidence/d1.3-inversion.txt`؛ والأحمر
نفسه: `d1.3-red.tap` (مع `d1.3-red-summary.txt` لقططة الشجرة غير المُصلحة) مقابل
`d1.3-green.tap` (6/6).

**بوابة الإغلاق**: `npm test` 637/637، `npm run metrics` أخضر (والوثائق تُطابق السجل:
`live_measurement.tests = 637`)، `npm run verify` أخضر، `git diff --check` نظيف،
`git diff --stat -- packages/ spec/` فارغ — لا سطر في النواة. DI-07 مُقلَبة إلى enforced
وجدول الملخص مُحدَّث؛ `gaps.json` تُشير حيًا إلى `tests/gates-computed.test.js`؛ ومُعيد
known-gaps/D1.3 حُذف في نفس التغيير.

### O10. تسجيل مصدر O09 وفجوة D1.17 — أعداد مُروَّاة تُخدم وتُزرَع وتُطبع (2026-09-23)

بعد إغلاق D1.12 (الوثائق وبوابة النشر) أظهر فحص `tools/celia-dashboard-server.mjs` — وهو
السطح الذي يقرأه operator فعلًا — أن نفس الفئة لم تكن موثّقية فقط:

| الرمز | مصدر التشغيل | الإسناد |
|:---|:---|:---|
| **O09** | أرقام وحكم ترقية مُروَّاة على حدود مخدومة: حالة HTTP ثابتة، وbeliefs/ذكريات مزروعة، وسجل إقلاع، وبيانات أولية في الواجهة | `tools/celia-dashboard-server.mjs:738-742,663,689-690,943-947,3124` + `dashboard/src/components/NexaDashboard.jsx:252` |

**الحقيقة المقيسة، لا المستنتجة**: `GET /api/celia/state` يعيد `mockState` حرفيًا، وكتلته تحمل
`tests:'314/314'` و`promotion:'5/5 READY'`؛ وسجل الإقلاع في نفس الملف يطبع `Tests: 501/501` —
رقمان مُختلَقان متناقضان في ملف واحد. وفيه ما هو أسوأ من العرض: `seedGovernedMemory` يزرع
`belief` شرطيّ الدليل `{gates:'6 CLOSED', tests:'314/314'}`، و`seedSemanticMemory` يزرع
«Security gates 6 CLOSED, 314 tests…» و«PASTE 48.5% latency saved» — فالعدد لا يُعرض فقط، بل
يُدخَل معرفةً في المحرك. وبالمقابل `/api/v1/system/status` (سطر 987) **يقيس** عبر
`getPosture()`: نفس السيرفر يُعرِّض سطحًا واحدًا لحارسين، أحدهما يقرأ والآخر يروّي.

**لماذا فُتحت ولم تُغلق الآن**: الفجوة مُسجَّلة في `gaps.json` (D1.17، `reproducer:"test"`) مع
مُعيد أخضر-كإثبات في `tests/known-gaps/d1.17-served-counts.test.js` — أربعة اختبارات: واحد
سلوكي فوق HTTP معزول (الخادم يُخدم فعلاً ويُطابق الحرف)، وثلاثة على المصدر (تناقض الإقلاع،
المزروعات، الواجهة). لا سطر إصلاح بعد: الترتيب المتفق عليه هو إتمام D1.4–D1.8 قبل توسيع
الجبهة، والقياس المُدخل اليوم في المجموعة يرفع عددها (641) وهو ما سيجبر — عبر حارس D1.12 —
تحديث `live_measurement` والوثيقتين في نفس التغيير، لا عند الإغلاق.

**ما لن تفعله التذكرة عند الإغلاق (نطاق مسبق)**: لا لمس لسلوك الجدار والمصادقة (مغلق في D1.10)،
ولا لحارس الاستمرارية (D1.11)، ولا تفكيك لملف السيرفر ذي الـ 3125 سطرًا (Phase 1 خارج النطاق)؛
والفعل المطلوب محدود: مصدر قيمة معلن، تناقض مُزال، مزروعات بلا أرقام أداء، وحارس يكسر عند
عودة الحرف.

### O11. سجل إغلاق D1.4 (P0-D / A09 / DI-06) — سؤالان، مسندان لا مسند واحد (2026-09-23)

**ما وُجد في الكود (لا كما وُصف في التسجيل)**: `GATE_3_DEMONSTRABLE_IMPACT` كان سطرًا واحدًا
`pass: ['CRITICAL','HIGH','MEDIUM'].includes(finding.severity)`، و`GATE_6_BOUNDARY_BYPASS` سطرًا
آخر `pass: finding.severity === 'CRITICAL' || ... 'HIGH' || ... 'MEDIUM'` — نفس القرار بصيغتين.
النتيجة المقيسة: «الأسئلة السبعة» خمسة عاملة، وأي finding بشدة تقريرية يحصل على إجابة «نعم»
لسؤالَي الأثر وعبور الحدود معًا دون أن يسمّي أثرًا ولا حدًّا. وحيث إن الجسر يشترط 7/7
(D1.2/D1.3)، كان هذا الطريق الأرخص لتمرير أي finding: رفع الشدة، لا تقديم الدليل.

**الإصلاح الأدنى**: مسندان منفصلان بالحقول وبالدلالة.
`src/security/hunter/impact-evidence.js` يحكم GATE_3 على **مضمون** `finding.impact`: عبارة
لا تقل عن 60 حرفًا، تسمّي متجه ضرر من جدول مغلق (`data|privacy|financial|privilege|availability|
integrity|network`)، ولا تصيغ نفسها احتمالًا (`may|might|could|potentially|theoretically|…`) —
فـ«قد يؤدي نظريًا إلى…» يبقى مرفوضًا ولو كان CRITICAL. و`src/security/hunter/boundary-evidence.js`
يحكم GATE_6 من سجلّ `finding.boundary = {from,to,kind}`: طرفان مختلفان غير فارغين، و`kind`
مصرّح به **لصنف الثغرة** في `BOUNDARY_KINDS_BY_CLASS`؛ والصنف غير المُسجَّل مرفوض ولو كان
سجلّه متماسكًا (لأن «بنر إصدار» لا يعبر حدًّا مهما بالغنا في ترتيبه). ولا أحد منهما يقرأ
`severity` — وهذا هو الفصل المطلوب: الشدة ترتيب أولوية، لا دليل.

**ما لُمس عند المنتِجين**: كل كاشف في `VulnEngine` يرفق الآن سجل حدوده صراحةً (IDOR:
`authenticated caller → object owned by another principal / authorization`؛ SSRF: `user-controlled
parameter → server-side network location / network`؛ CORS: `attacker-controlled origin → victim
origin's authenticated response / origin`؛ Injection: `unvalidated input → query interpreter /
data`؛ Race: `concurrent request A → shared state committed by request B / state`). وأثر
`SQL_NOSQL_INJECTION` كان ينتهي بـ«and potential RCE» — حُذف القيد المتردد من عبارة الكاشف
بدل تليين المُقيِّم.

**الإثبات**: `d1.4-red.tap` — الحارس الدائم `tests/gate-boundary-vs-impact.test.js` ستة من ستة
حمراء عند `6699586` بإصلاح معكوس (src وحده، والحارس موجود). `d1.4-green.tap` — ستة من ستة
خضراء على الكود النهائي. `d1.4-inversion.txt` — تعطيل مسند الأثر وحده (GATE_3 ← severity)
يُحمرّ D1.4 وحده (3 من 6)، وتعطيل مسند الحدود وحده (GATE_6 ← severity) يُحمرّ D1.4 وحده
(4 من 6)؛ وفي الحالتين `gates-computed` ‏6/6 (D1.3) و`artifact-reader` ‏16/16 (D1.2)
و`agentic-hunter` ‏10/10 ومُعيدات D1.5/D1.6/D1.7 خضراء. المجموعة الكاملة عند الإغلاق
**645/645/0** (641 عند تسجيل D1.17 + 6 للحارس − 2 لمُعيد `known-gaps/d1.4` حُذف).

**تصحيح اضطراري داخل قالب D1.2 (بلا تليين)**: حالة «لا إيصال ببوابات أقل من 7/7» كانت
تثبّت عدد الساقطة (`'6/7'`) اعتمادًا على أن صنف never-submit يسقط GATE_5 وحدها؛ ومن D1.4
يسقط معها GATE_6 أيضًا. أُبدل التثبيت بعدّ الساقطين بتثبيت أن الجسر لم يصل 7/7 وأن الرفض
سمّى GATE_5 بعينه — وهو ما تختبره الحالة فعلًا؛ والحالة الثانية (gateCheck مزوَّر) أُسقِطت
بـ`artifact: undefined` (يسقط تحت أي نظام بوّابات) بدل الاعتماد على شدة منخفضة. لم يُخفَّف أي
assert، ولم تُقبل حالة كانت مرفوضة.

**ما لم يُفعل عمدًا**: لا بوابة ثامنة ولا دمج للسؤالين — بقيا سؤالين بمسْنَدَين؛ لا تعديل
لقاعدة 7/7 ولا لصيغة `NEXA-E-GATE`؛ لا مساس بـ`packages/` ولا بـ`spec/` (`git diff --stat`
عليهما فارغ)؛ ولا توسيع لجدول الأصناف ليلائم كاشفًا — الجدول يرفض افتراضيًا والصنف الجديد
يُضاف صراحةً عند وجوده. **القصور المتبقي موثّق**: `impact` ما زال ادعاءً نصيًا يصرّح به
المُنتِج، لا سجلًا موقّعًا مربوطًا بالدليل كما صار `safeTesting` في D1.3؛ وربط الأثر بالدليل
مؤجَّل لتذكرة مستقلة إن طُلب تشديده.

### O12. سجل إغلاق D1.5 (P0-D / A12 / DI-13) — النطاق يُطابَق، ولا يُروى (2026-09-23)

**ما وُجد عند المنتِج والمُقيِّم والجسر**: ثلاث نقاط كانت تُغلِق السؤال الأول بلا إجابة.
`seven-gate-validator.js` يسأل `context.inScope !== false` — أي أن **عدم المعرفة ترخيص**؛
`nexa-evidence-bridge.js:100` يعوّض غياب السياق بـ `{ inScope: true }`؛ و`autopilot.js:55` كان
يمرّر هذا الحرفي إلى التصفية بعد أن أجرى عند `:39` فحص `isScopeAllowed(target)` حقيقيًا — أي أن
النتيجة الجيدة حُسبت ثم رُميت، وبقي ادعاؤها. وبما أن قاعدة 7/7 (D1.2/D1.3) تطلب كل البوابات، كان
هذا أرخص طريق لتحويل أي finding إلى إيصال موقّع: قلْ «inScope: true»، ولا تُثبت شيئًا.

**الإصلاح الأدنى**: `src/security/hunter/scope-evidence.js` يحكم GATE_1 من سجلّ بيانات
`{target, allow[], deny[]}` **يعيد المُقيِّم حسابه بنفسه**: لا سجل ⇒ رفض صريح؛ `allow` فارغ أو
غير مصفوفة ⇒ رفض (قائمة فارغة ليست «كل شيء مسموح»)؛ عيب في أي نمط (`*`، `.*`، `.com`، `com`،
`*.` الفارغة، نص لا نقطة نطاق فيه) ⇒ رفض يسمّي العيب؛ الهدف لا يطابق allow أو يقع في deny ⇒
رفض؛ ثم يُطابَق **أصل الـ finding**: إن ذكر `endpoint|url|host|asset` مضيفًا يختلف عن الهدف رُفض
(`finding.asset لا يستعير ترشيح هدف الجولة`)، وإن كان مسارًا في وضع المسار فلابد أن يحلّ داخل
جذر مُصرَّح به — فـ`/repo/../etc/passwd` يخرج من الترخيص، والجذر `/` مرفوض كنطاق.
وفي الوضعين: `finding.outOfScope` و`context.inScope === false` و`scope.authorized === false`
قاطعة — المستدعي يضيّق ولا يوسّع.

**ما لُمس عند المستهلكين**: الجسر صار `options.gateContext ?? {}` (لا سجل مُهدى)؛ autopilot يبني
`{ scope: this.reconAgent.scopeRecord(target) }` **مرة واحدة** ويستعمله في التصفية والإيصال معًا
حتى لا يختلف «مسموح بالصيد» عن «مصدَّق عليه»؛ `ReconAgent.scopeRecord` صار القناة الوحيدة التي
تُسلَّم بها الأنماط للمُقيِّم. و`AgenticHunter` (جولة مصدر محلي) صار يصرّح بجذره: `{target:
this.targetDir, allow: [this.targetDir]}` فتُطابَق ملفات الـ findings عليه. لم تُغيَّر إباحة
`isScopeAllowed` عند غياب الأنماط: البقاء على «يُسمح بالعدّ» مع «لا يُصدَّق على شيء» هو الفصل
المطلوب، لا توسيعًا في الاستطلاع.

**الإثبات**: `d1.5-red.tap` (8 من 8 حمراء عند `9cda12b` قبل أي تعديل src) → `d1.5-green.tap` (8/8)
→ `d1.5-inversion.txt` (إعادة GATE_1 إلى الثقة boolean تُحمرّ حارس D1.5 وحده 0 من 8؛ وردُّ
الافتراض المُهدى في الجسر يُحمرّ اختبار الجسر وحده 7 من 8؛ وفي الحالتين `gates-computed` 6/6،
`gate-boundary-vs-impact` 6/6، `artifact-reader` 16/16، `agentic-hunter` 10/10، ومُعيدا D1.6/D1.7
خضراء) → `d1.5-suite.txt` (المجموعة الكاملة عند الإغلاق).

**ثمن صادق دُفع في قوالب التذاكر المغلقة**: 11 حالة في أربعة ملفات كانت تعتمد الهدية (خمسة منها
في D1.2/D1.3 كانت ستصبح **خضراء بالكسل** — `findingDigest` لرفض لا إيصال، فتنجح مقارنة
`undefined === undefined` من غير أن تثبت شيئًا). استُبدل `{ inScope: true }` بسجل نطاق صالح في كل
موضع، وأُعطيت حالة «different target» سجلها المطابق حتى تبقى مقارنة الـ digests حقيقية. لا assert
خُفِّض، ولا حالة انتقلت من مرفوضة إلى مقبولة بلا دليل مُضاف.

**ما لم يُفعل عمدًا**: لا ملف نطاق خارجي مُصدَّق (قائمة النطاق يصرّح بها المشغِّل)، ولا توقيع
للسجل، ولا منع لجولات عداء غير مُرخَّصة في الاستطلاع (التصديق هو الحاجز)، ولا تغيير لـ`packages/`
ولا `spec/` (`git diff --stat` عليهما فارغ)، ولا بوابة ثامنة. **القصور المتبقي موثّق** في
`follow_ups` للتذكرة: مصدر قائمة النطاق، وارتباط الأثر بالدليل (مؤجَّل من D1.4).

### O13. سجل إغلاق D1.6 (P0-D / A11 / DI-14) — ذاكرة لا تبتلع، لأنها لو ابتلعت لَمحتَ (2026-09-23)

**الأثر الذي لم يكن في نص التذكرة**: القراءة كانت تبتلع أي استثناء بلا تمييز
(`catch { // Fallback }` → ذاكرة فارغة)، والكتابة كانت تبتلع فشل النظام بسطر `console.error` فقط.
النتيجة ليست «نسيانًا» بل **مسحًا قابلًا للحدوث**: ذاكرة فارغة تُحفظ فوق الملف الفاسد، فتضيع
الجلسات السابقة من غير أي إشارة — والابتلاع في `save` يجعل هذا غير قابل للاكتشاف حتى للمُشغِّل.
لذلك كان الإصلاح الجوهري هنا ليس «أضِف سجلًا» بل **امتناع الكتابة فوق مخزن لم يُقرأ**.

**العقد الجديد**: `loadStatus` يفرّق أول تشغيل (`{ok:true, fresh:true}` — ليس فشلًا) عن
`NEXA-HM-READ-FAILED` (المسار دليل/غير قابل للقراءة) و`NEXA-HM-CORRUPT` (ليس JSON) و
`NEXA-HM-MALFORMED` (JSON صحيح لكنه ليس بنية ذاكرة: مصفوفة، `null`، `sessions` ليست مصفوفة…) —
فما كان يُعاد تفسيره «فارغًا» صار مرفوضًا صراحةً. وعند `!loadStatus.ok` ترفض `save()`
بـ`NEXA-HM-REFUSED-OVERWRITE` ولا تلمس البايتات؛ الاستعادة قرار مُوقَّع نيّةً: `save({force:true})`
وحده يكتب، وتُوسَم الحالة بـ`recovered`. وفشل الكتابة `NEXA-HM-WRITE-FAILED` مع `errno`/`syscall`،
والكتابة صارت ذرّية (tmp + rename، ولا ملف مؤقت متروك بعد الفشل — يُبلَّغ إن تعذّر تنظيفه)، وكل
نقطة كتابة (`recordSession`, `rememberTarget`, `save`, `garbageCollect`) تُرجع حالة بدل undefined.
المنسِّق يتصرّف: `autopilot` صار يخزّن `result.memory = {persisted, code, path}` ويحذّر عند الفشل،
**من غير أن تُصبح الذاكرة بوابة**: في الاختبار تُصدَر الإيصالات كاملة مع ذاكرة مرفوضة، لأن خلل
تخزين لا يمنح ثقة ولا يسحبها.

**الإثبات**: `d1.6-red.tap` (6 من 6 حمراء عند `86207c3` قبل أي تعديل src) → `d1.6-green.tap` (6/6)
→ `d1.6-inversion.txt` بثلاث نسخ، كلٌّ منها تُحمرّ حارس D1.6 وحده: إلغاء الامتناع عن الدهس (2 من 6
حمراء)، وابتلاع `_load` (5 من 6)، وتزفير فشل الكتابة نجاحًا (1 من 6) — ومعها scope-independence
‏8/8، gate-boundary 6/6، gates-computed 6/6، artifact-reader 16/16، agentic-hunter 10/10،
test-output-isolation 3/3، ومُعيد D1.7 2/2. المجموعة الكاملة عند الإغلاق **655/655/0**.
وقد تبيّن أن المخزن المرفق في المستودع (`dashboard/data/hunt-memory.json`، 9 جلسات) يُقرأ
`ok:true` تحت العقد الجديد — فلا إنذار كاذب على حالة قائمة.

**ما لم يُفعل عمدًا**: لا تغيير لمعرفات الجلسات (ما زالت `session_${Date.now()}`) رغم أن مُعيد
التذكرة كان يصفها — ليست من معايير D1.6 المسجلة، فصارت متابعة موثّقة تُغلق مع هوية/حتمية §6.3 في
تذكرة مستقلة؛ لا قفل تعدد العمليات؛ `force` لا يسجّل من فعله؛ ولا مساس بـ`packages/` ولا `spec/`.

### O14. سجل إغلاق D1.7 (P0-D / A05 + A06 / DI-01، DI-15، DI-16) — الكاشف وحدة، والحكم في نطاقه (2026-09-23)

**الحقيقة المقيسة**: ثلاثة كواشف كانوا داخل جسم المنسِّق (`agentic-hunter.js:81-116`)، وشرطان
منها يُقيَّمان على **محتوى الملف كاملًا**: `async && !content.includes('try {') && !content.includes('.catch(')`
و`!content.includes('.close') && !content.includes('.destroy')`. الأثر ليس أسلوبًا بل نتيجة:
`try` في دالة لا علاقة لها كان **يُسكِت كشفًا حقيقيًا** (false negative مضمون البنية)، وكل سطر
`async` كان يعيد دفع نفس الحكم (تكرار)، ولم يكن لأي finding دليلٌ ولا شهادة — فتُرفض عند
GATE_2/GATE_7 مهما كانت صوابًا.

**الإصلاح**: ثلاث وحدات نقية (`detector-unhandled-async.js`, `detector-weak-crypto.js`,
`detector-resource-leak.js`) تُصدِّر دالة تُدعى بمدخلات معلنة `{relativePath, content}`: لا
نداءات `fs.*`، لا `process.cwd/env`، لا حقل لحظي — فتُستدعى مرّتين بناتج متطابق وتُختبر منفردة.
الحكم صار في نطاق هدفه: أول `await` **داخل الدالة نفسها**، وجسم النطاق **مُقنَّع** بنطاقاتها
الداخلية (الدالة الداخلية مسؤولة عن أخطائها، والخارجية لا تُحاسَب عليها ولا تُرحَم بها)؛ والإغلاق
محسوب **لنفس المتغير** في نطاق إطلاقه — `closeSync(fd)` لـ`fd`، لا لأي `.close` في الملف؛ وموردان
في سطر واحد = حُلمان. قائمة تجميع واحدة `SOURCE_DETECTORS` (يُصدِّرها `index.js`) هي ما يستهلكه
المنسِّق: `for (const detector of SOURCE_DETECTORS) for (const f of detector.detect({relativePath, content}))`
— لا صنف كشف ولا شرط ولا بناء يدوي داخله.

**الدليل، لا الحكم**: `detector-evidence.js` يبني لكل finding `artifact` من نوع `content-match`
بـ`locator = file:<rel>#L<line>` و`evidence.scope` يسمّي الدالة وحدودها، مع `safeTesting` مربوطة
بنفس المُنتِج (عقد D1.3) — بلا `verdict` ولا `passed` ولا `confidence`: الحكم للمُقيِّم وحده.
وبالتالي تُستوفى GATE_2/GATE_7/GATE_1 **فعليًا**، وتبقى `isValid === false` لأن GATE_6 ترفض صنفًا
معلوماتيًّا/جودة لا يعبر حدًّا. ولم يُوسَّع جدول `BOUNDARY_KINDS_BY_CLASS` على المقاس — الحارس
يتحقق من ذلك بنيويًا (`hasOwnProperty` على صنف كل كاشف) ومن أن الكاشف لا يبتكر `finding.boundary`:
فصل الوحدات لا يُشترى بتمرير البوابات.

**تصحيح لاصق مقصود**: كان `relativePath = path.relative(process.cwd(), filePath)` — وعلى شجرة خارج
cwd يصير الأصل `../../../tmp/...`، أي مسارًا يبدأ خارج الجذر عند GATE_1 (D1.5) يمرّ بالاتفاق
العَرَضي لا بالدلالة. صار الأصل نسبةً إلى **جذر الجولة** (`path.relative(this.targetDir, filePath)`).

**الإثبات**: `d1.7-red.tap` (7 من 7 حمراء عند `7ad20f1` — الحارس يحمّل الوحدات ديناميكيًا فيفشل
كل اختبار على ادّعائه، لا على خطأ استيراد يُلغي الملف) → `d1.7-green.tap` (7/7) →
`d1.7-inversion.txt` بثلاث نسخ، كلٌّ منها تُحمرّ حارس D1.7 وحده: عودة الشرط على الملف كله (5 من 7)،
حذف الشهادة من الباني المشترك (4 من 7)، تفريغ قائمة التجميع (5 من 7) — ومعها scope-independence
‏8/8، gate-boundary 6/6، gates-computed 6/6، artifact-reader 16/16، agentic-hunter 10/10،
hunt-memory 6/6. المجموعة الكاملة عند الإغلاق **660/660/0**، ومُعيد التذكرة حُذف (اختباراه كانا
أخضر-كإثبات قبل الإصلاح وأصبحا حمراءَ بعده: إشارة صحيحة لأن الفجوة أُغلقت).

**ما لم يُفعل عمدًا**: لا AST (مطابقة أقواس حرفية، بقصورها الموثّق)، ولا توسيع لأنماط الكشف
(التشفير الضعيف بقي `createHash(md5|sha1)` كما كان — ما صُحِّح هو موضِع الحكم وحَمْله للدليل)،
ولا `SecretsHunter` (يُصدِّر findings بلا artifact — نفس الداء في وحدة جيدة البنية، سُجِّل متابعةً
لتذكرة مستقلة)، ولا تعديل للبوابات أو لـ`packages/` ولا `spec/`.

### O15. سجل إغلاق D1.8 (P0-D / A13 / DI-17) — التقرير لا يعرض إلا ما صُدِّر له إيصال (2026-09-23)

**الحقيقة المقيسة** (من `f7459bd`، لا من صياغة التقرير الأصلي): `_generateReport` كان يخلط
بين ثلاث حالات مختلفة في رقم واحد. (1) `details = triage.validated.map(...)` — كل مُصرَّح به
عند المُقيِّم يدخل التقرير **مهما كانت نتيجة إيصاله**، فرفض الجسر المستقل لا يمنع النشر؛ وحين
قيس الأمر تبيّن أن إيصال النجاح يحمل `verified: true` + توقيعًا ولا يحمل `certified: true`
(الحقل `certified:false` يظهر في الرفض وحده) — فعبارة «certified:false كان يُنشر» صحيحة
المعنى، لا صحيحة الحقل: الوصول إلى الرفض اليوم ممكن بحقن جسر أو بفشل تجهيز المفتاح، لا
بتشغيل اعتيادي؛ سُجّل ذلك أمانةً بدل تضخيمه. (2) `critical/high/medium/low` تُحسب على كل ما
أُبلِغ قبل البوابات، ولا حقل يقول ذلك — فـ `totalIssues: 12` تُقرأ «12 حكمًا». (3) السطر
المطبوع `Saved to dashboard/data/bug-report.json` حرفي بينما الكتابة إلى `reportPath`
(بـ `NEXA_BUG_REPORT`)، فكل تشغيل اختبار يطبع مسارًا لم يُكتب فيه شيء، وفشل الكتابة كان
يُبتلع. (4) `report-writer.js:42` يوقّع كل markdown بـ `(7-Question Gate: 7/7 PASSED)` و
«cryptographically pre-validated» دون أن يقرأ `finding.receipt` — ادّعاء تشفيري لـ finding
قد لا يكون له إيصال أصلًا.

**الإصلاح** (في `src` وحده؛ `packages/` و`spec/` لا تماس): `details` تُصفّى إلى ما صدر له
إيصال فعلًا، والمرفوض يذهب إلى `refused` مع `code` و`reasons`؛ `counts = {asserted, validated,
certified, refused}` مع تثبيت `certified === details.length`؛ كل detail يحمل `findingId`
و`artifactDigest` من إيصاله (علاقة بالمعرّف، لا بالاسم)؛ أساس الأرقام معلن (`severityBasis:
'asserted'`) بجانب `certifiedSeverity` المحسوبة على المُصدَّق وحده؛ الكتابة صارت ذرّية
(tmp+rename) بحالة `persistence = {ok, path, bytes}` تُبلَّغ في التقرير والسطر المطبوع يذكر
المسار الحقيقي، والفشل `NEXA-REPORT-WRITE-FAILED` بسببه؛ وتذييل report-writer يُشتق من
`finding.receipt` (معرّف الإيصال + درجته + بصمة توقيعه، أو `NOT CERTIFIED` مع الرمز
والأسباب). `AutopilotEngine` يخضع للعقد نفسه: `reports` و`validatedFindings` للإيصالات الصادرة،
و`counts` و`refusedFindings` مرئية. `totalIssues` بقي بمعنى المُبلَّغ — **إضافة لا استبدال**،
التزامًا بقيد التوافق.

**الأدلة**: حمراء-كإثبات 4/4 عند `f7459bd` (`self-model/evidence/d1.8-reproducer.tap`)، وحارس
دائم `tests/bug-report-consistency.test.js` 6 من 6 حمراء قبل الإصلاح (`d1.8-guard-red.tap`) ثم
7 من 7 بعده (`d1.8-green.tap`)؛ المجموعة الكاملة **667/667/0** مع `npm run verify` exit 0
و`npm run metrics` أخضر (`d1.8-suite.txt`). الانحراف الرباعي (`d1.8-inversion.txt`): إعادة
`details` بلا فلتر ⇒ اختباران من سبعة حمراء؛ ادّعاء حفظ رغم فشل الكتابة ⇒ واحد؛ استعادة
«7/7 PASSED» الثابت ⇒ اثنان؛ تقارير لكل مُصرَّح به في autopilot ⇒ واحد — وفي الأربع بقيت حراس
D1.2–D1.7 خضراء (7/7، 8/8، 6/6، 6/6، 16/16، 10/10، 6/6، 3/3). حارس التذكرة يحمرّ وحده، وهي
علامة أن الحظر خاص بالتذكرة لا مصادفة عابرة.

**ما لم يُفعل عمدًا**: لا توحيد لاسم حقل النجاح (`verified` بدل `certified`) لأنه كسر توافق؛
لا سياسة تجهيز مفاتيح توقيع (الجسر لا يزال يولّد زوجًا عابرًا عند غياب مفتاح، فـ «رفض بفشل
مفتاح» غير قابل للقيس تشغيليًا — سُجّل متابعة)؛ لا مخطط JSON لـ `bug-report.json`؛ ولا توسيع
لكشف (`SecretsHunter` يبقى يُصدِّر findings بلا artifact فتُرفض عند GATE_2، وذلك مرئي الآن في
`counts.asserted > counts.validated` لا مُموَّهًا). وبقلب DI-17 إلى enforced تكتمل مجموعة
**P0-D** (D1.3–D1.8)؛ المتبقي من صفر-الحرج المُسجَّل: D1.17 (قيد التسجيل، قياسٌ حيّ فقط).

**أمانة provenance**: رسالة `f7459bd` حملت رمزًا صينيًّا شاردًا (`于事`) في متنها من جرف نصّي؛
لا ملف تأثر (فحص الشجرة نظيف)، والمدفوع لا يُعدَّل — فيُسجَّل هنا بدلًا منه. مُعيد D1.8 كان
غير متتبَّع طيلة دورته فلم يدخل أي عدّاد سابق، وحُذف بعد أن حُمِّل إثباتًا؛ الحساب 667 = 660 + 7.

### O16. سجل إغلاق D1.17 (P1-A / O09) — السطح المخدوم يقيس أو يصمت (2026-09-23)

**الحقيقة المقيسة**: هذه هي فئة D1.12 ذاتها، لكنها انتقلت من الوثيقة إلى الحمولة الحيّة.
`mockState.status` كان يحدّد `gates:'6 CLOSED'` و`tests:'314/314'` و`promotion:'5/5 READY'` و
`llm_vectors:'2/2 BLOCKED'` حروفًا، و`GET /api/celia/state` يعيدها كما هي؛ بينما شقيقه
`/api/v1/system/status` في الملف نفسه يقيس فعلًا عبر `tools/check-posture.mjs` — حارسان لسطح
واحد، أحدهما يقيس والآخر يروّي. والروايتان متناقضتان داخل الملف نفسه: 314 في الحالة المخدومة،
501 في سجل الإقلاع، وكلاهما لا يحدّثه أي قياس (تُحقّق من غيابه: `mockState.status` لم يُسنَد من
أي مصدر إطلاقًا). والأرقام لم تكن عرضًا: `seedGovernedMemory` يزرع `condition:{gates,tests}`،
و`seedSemanticMemory` يزرع «Security gates 6 CLOSED, 314 tests…» و«PASTE 48.5% latency saved»
فتصير روايةَ المحرك معرفةً، والواجهة تحمل `testsPass:'314/314'` بياناتٍ أولية تُعرض قبل أي
response.

**الإصلاح، بشكله الذي قرّره المالك (قياس حيّ فقط)**: `measurePosture()` ينفّذ `check-posture.mjs`
ويحلّل أسطر `NEXA_METRIC k=v` (عيّنة تُخبَّأ ثانيتين لأن اللوحة تُستطلع بالتكرار، و`measured_at`
يصرّح بوقت العيّنة)، و`servedStatus()` تُعيد **القياس فقط** — `gates` وأعداد البوابات/الفضاءات/
الأفعال/المراحل/الأصناف/الوحدات/الرموز/الخطوات/الحالات — بلا `tests` ولا `promotion`: هذا السيرفر
لا يقيس الاختبارات ولا الترقية، وحذف الادعاء أولى من وسمه برقم قديم، وتجميد `baseline` وتحويله
تليمترية عبر الشبكة مرفوض صراحةً. وعند تعذّر القياس **تُخدَم الكتلة بلا عدد** مع
`measurement_error: NEXA-POSTURE-UNAVAILABLE|UNMEASURABLE` — ولا جواب محفوظ: الحرف الذي يصادف أنه
صحيح اليوم ليس قياسًا. النثر التسويقي نُقل إلى `claims` موسومًا `kind:'claim'` + «not verified by
any check» (بما فيه «100% fix») فلا يُقرأ تليمترية. سجل الإقلاع يطبع قراءة `servedStatus()` نفسها
ويعلن صريحًا أن عدد الاختبارات وحكم الترقية لا يقيسهما هذا Process. المزروعات صارت إحالة إلى
القياس (`condition:{posture:'measured by …'}`)، والحقائق الدلالية بلا «314 tests» ولا «48.5%»،
و`pasteSaving` رواية موسومة في السيرفر والأداة التجريبية معًا. والواجهة: `testsPass`/
`securityGates`/`contextUsage` صارت `'not measured'`، و`memoryDigests`/`evidenceCount` صفرًا حتى
يأتي response، وسطر «6 gates CLOSED … Z3 100% proof» صار «engine claims — not measured».

**الأدلة**: حارس `tests/served-telemetry-honesty.test.js` 6 من 6 حمراء قبل الإصلاح
(`d1.17-guard-red.tap`) — وسابعها (تطابق القياس في جذر فيه `spec/`) كُتب معه — ثم 7 من 7
(`d1.17-green.tap`)؛ المجموعة الكاملة **670/670/0** مع `npm run metrics` أخضر و`npm run verify`
exit 0. الانحراف الخماسي (`d1.17-inversion.txt`): جواب محفوظ عند تعذّر القياس ⇒ 3 من 7؛ حروف في
تعريف الحالة ⇒ 2 من 7؛ `314/314` في الواجهة ⇒ 1 من 7؛ العدد معرفةً مزروعة في belief ⇒ 2 من 7؛
سجل إقلاع يروّي 501/501 ⇒ 1 من 7 — وفي الخمس بقيت أسوار D1.10/D1.11 و T*-commit خضراء (57 اختبارًا:
perimeter 9/9، rate-limit 7/7، failfast 6/6، startup-guard 8/8، commit-http 3/3، commit-auth 19/19،
hardening 3/3، workspace-http 1/1، write-auth 16/16، output-isolation 3/3). الاختبار الثالث في
الحارس هو الجوهر: في بيئة لا يمكنها القياس يجب **غياب** العدد، فذلك ما يميّز «يقيس» من «يحفظ الجواب».

**ما لم يُفعل عمدًا**: لا تمرير `baseline.live_measurement` إلى HTTP (مرفوض بقرار المالك — سجلٌّ
مجمّد لا يصير تليمترية بالشبكة)؛ لا `llm_vectors` مقيسة (لا قياس داخل العملية؛ `npm run vectors`
يقيسها، وربطها تذكرة تليمترية مستقلة)؛ لا وسم بالأرقام القديمة مع تاريخ؛ لا تفكيك للسيرفر ولا
Phase 1؛ ولا مساس بجدار D1.10 ولا بحارس D1.11؛ ونثر صفحة الهبوط (`v0.9-infinite`، «56 Engines»،
«Z3 100% proof» في HTML) وترويسة `v0.8 ultimate // 8-Tier + 7 Physics` ادّعاءات قدرة لا أعداد
حالة — سُجّلت متابعة لا توسيعًا هنا. وبهذا تُغلق آخر تذكرة «أعداد مُروَّاة» مُسجَّلة، وتبقى
known-gaps للأربعة D1.13–D1.16 (مفتوحة كما قُنِّنت).

### O17. سجل إغلاق D1.13 (P1-B / O05) — مرجع البيئة وثيقةٌ إلزامية، لا قالب مُودَع (2026-09-23)

**الحقيقة المقيسة**: كان في الشجرة 20+ قراءة `process.env` موزَّعة على أربع طبقات (محيط، حدّ معدل،
حارس إقلاع، سيرفر، مُكيِّفات، صائد أخطاء، ذاكرة صيد) بلا وثيقة تسمّي واحدًا منها، وبلا أي فحص يمنع
أن تنمو القراءات صامتة. المُعيد المسجَّل صاغ الفجوة بغياب `.env.example`؛ القياس بيّن أن غياب
القالب **قرارٌ لا فجوة** (§3 من سجل الحدود المُجمَّد: قالب مودَع يدعو إلى إيداع سرّ فيه)، وأن
الفجوة الحقيقية هي انعدام المرجع المُلزِم — والدليل أن لا شيء في `tests/` كان يقرأ المرجع أصلًا.

**القرار (تعارض محلول لا تفويض جديد)**: نصّ التذكرة كان يطلب `.env.example`؛ غُيِّر الشكل إلى
`docs/environment-reference.md` + حارس مزامنة، فجوهر المطلوب (مرجع مركزي إلزامي لكل متغير مع
القارئ والافتراضي والأثر والصنف) يتحقق دون نقض القرار الأمني. سُجّل في `decision_log` للتذكرة.

**الإصلاح**: المرجع بصفوف خماسية الأعمدة، وقسم «قراءات غير مباشرة» يسمّي ملفات `process.env[key]`
(حافظات demos/grok/rag) فلا يتكاثر الصمت، وقسم «ما ليس متغيّر بيئة» يصرّح أن `SUPABASE_KEY` مفتاح
حمولة لا قراءة، وأن `NODE_VERSION` إعداد ناشر لا `process.env`. والحارس
`tests/env-reference-sync.test.js` يفرض: كل قراءة موثَّقة؛ لا صفّ جامد (العذر الوحيد أن يُعلَن داخل
قسم غير المباشر نفسه — ذكر عابر في الوثيقة لا يُبرّئ); كل موقع `process.env[` مُدرَج بالمسار؛
لا `.env.example` والمرجع يشرح سبب الرفض ولا يعطي قيمة سرّ حقيقية؛ لا `process.env` ولا
`import.meta.env.VITE_*KEY|TOKEN|SECRET|SEED` في `dashboard/**` **بما فيه `.jsx`/`.html`** — لأن
الحصر في `.js/.mjs` كان يترك الثغرة بالاتساع نفسه (ثغرة أغلقت أثناء كتابة الحارس لا بعدَه).

**الأدلة**: حمراء-كإثبات 2 من 2 (`d1.13-red.tap`: لا مرجع، ولا حارس، و20 قراءة مُستنفَدة) ثم
6 من 6 (`d1.13-green.tap`)؛ المجموعة الكاملة **675/675/0** و`npm run metrics` أخضر. الانحراف
السداسي (`d1.13-inversion.txt`): قراءة بلا صفّ ⇒ 1 من 6؛ حذف صفّ مع بقاء القراءة ⇒ 1؛ صفّ جامد ⇒ 1؛
موقع قراءة غير مباشرة جديد ⇒ 1؛ عودة `.env.example` ⇒ 1؛ تسرّب `VITE_NEXA_API_KEY` إلى jsx ⇒ 1 —
وفي الستّ بقيت حراس D1.17 والمحيط والإقلاع والسجل والدخول خضراء (33 اختبارًا مرجعيًا).

**ما لم يُفعل عمدًا**: لا `.env.example` ولا أي ملف `env*` مودَع؛ لا قيم حقيقية في المرجع (الافتراضات
المذكورة كلها `mock-*`/`demo-*` منشورة)؛ لا فحص AST للقراءات الديناميكية (تُعلَن يدويًا ويُمنع
تكاثرها)؛ ولا توسيع إلى `spec/`. **تصحيح عرضي مُسجَّل للأمانة**: كان في `README.md` عدد جامد
`# 113 tests` لا يربطه أحد بقياس — حُذف إلى إحالة كتلة `NEXA_METRICS`؛ هو من فئة D1.12 لا D1.13،
فسُجِّل هنا بدل أن يُنسب إلى التذكرة الخطأ.

### O18. سجل إغلاق D1.14 (P1-C / O06) — الفهرس مُلزَم، والحذف ممنوع (2026-09-24)

**الحقيقة المقيسة**: 29 ملفًا ومجلد أدلة (45 ملفًا) في `docs/` بلا فهرس ولا صنف ولا مُحدِّث.
الأثر ليس جماليًا: `project-status-and-roadmap.ar.md` و`celia-system.ar.md` و
`nexa-remaining-phases-and-h2.ar.md` و«المانيفستو» تحمل أرقامًا مقيسة **لوقتها** (331/331،
415 باختبارَين معلَّمين، 428، «100% PASS»، قرار HOLD) ولا شيء في المستودع يقول إن هذه سجلّات
لا حالة — فأي قارئ يخلطها بـ`self-model/gaps.json`. الفئة نفسها التي أغلقتها D1.12 في
README/SECURITY، على سطح لم يكن له حارس: عدد لا يقارَن بقياس.

**الإصلاح**: `docs/README.md` بجدول أربع أعمدة (الوثيقة | الغرض | الصنف | المرجع) يغطي كل
مدخل أعلى `docs/` عدا نفسه، وبتصنيف مغلق: **سارية** (تُحدَّث مع الكود) / **سجلّ** (append-only
مؤرَّخ) / **تخطيط** (حالته في `self-model/gaps.json`) / **تصميم** (لا تدّعي تنفيذًا) /
**مؤرشفة**. وقاعدة صريحة: **لا حذف للتوثيق** — المنسوخ يُنقل إلى `docs/archive/` ويُحدَّث صفّه،
لأن الحذف يمحو الدليل. والحارس `tests/docs-index-sync.test.js` يفرض التزامن في الاتجاهين،
ويمنع صنفًا غير معلَن، ويسند كل مسار مذكور (حارسًا كان أو دليلًا) إلى ملف موجود، ولا يقبل
إحالة غائمة («كسابقها») في عمود المرجع، ويُلزِم السجلّ/التخطيط بتاريخ، ويطلب أن يُذكر الفهرس في
`CONTRIBUTING.md`/`README.md` كي لا يكون فهرسًا لا يُقرأ.

**الأدلة**: حمراء-كإثبات 2 من 2 (`d1.14-red.tap`: لا فهرس، ولا حارس فهرس، والسجلّ لا يصرّح بصنفه)
ثم 5 من 5 (`d1.14-green.tap`)؛ المجموعة الكاملة **679/679/0** و`npm run metrics` أخضر و
`npm run verify` exit 0. الانحراف الخماسي (`d1.14-inversion.txt`): وثيقة غير مفهرسة ⇒ 1 من 5؛
حذف بلا أرشفة ⇒ 2 من 5؛ صنف `قديمة` ⇒ 1؛ إسناد إلى `tests/env-reference-guard.test.js` غير
الموجود ⇒ 1؛ اقتلاع «لا حذف» من المقدمة ⇒ 1 — وحراس الوثائق والسجلّ والأدلة المجمّدة والتزامن
خضراء في الخمس (19 اختبارًا).

**أمانة القياس والتصحيحات العرضية**: (1) الحارس كُتب مرّتين — الأولى كانت تطلب مسارات جذرية
لأسماء نسبية إلى `docs/` وتحتجّ بميل المجلدات؛ صُحِّح لا الفهرس، لأن الفهرس كان صادقًا.
(2) `git rm` فشل أولًا لأن المُعيد كان معدَّلًا محليًا، فحُذف بـ `-f` بعد أن حُمِّل إثباتًا.
(3) صُحِّح قالب رسالة الـ commit في `CONTRIBUTING.md` («113 pass») إلى إحالة عدد الجولة، وأُضيفت
قاعدة مساهمة رقم 10 (الوثائق تُفهرَس ولا تُحذف) — كلاهما من فئة D1.12 لا D1.14، فسُجِّل هنا.
(4) **ما لم يُفعل عمدًا**: لا فهرسة داخل `docs/evidence/` ملفًا ملفًا (تتغيّر مع كل تذكرة)؛ ولا
توسيع قاعدة «لا عدد بلا قياس» إلى `docs/` كلها (يبقى تذكرة مستقلة، فالحارس الحالي يفحص
README/SECURITY فقط)؛ ولا أرشفة فعلية لوثيقة — من يُؤرشف لاحقًا يجد القاعدة والمسار والحارس.

### O19. سجل إغلاق D1.15 (P1-D / O07) — التكرار قرارٌ معلَن: مرآة مولَّدة أو كسر في CI (2026-09-24)

**الحقيقة المقيسة**: تسعة أزواج متطابقة بالبايت في الشجرة — سبع وحدات `adapters/edge-rag/*.js`
و`adapters/edge-rag/index.html` ↔ `dashboard/public/edge-rag.html` و`dashboard/workspace.html` ↔
`dashboard/public/workspace.html` — ولا شيء يقول أيّ الجهةين مصدر ولا ما يحدث حين تُصلَح واحدة.
التطابق الحالي **خداع**: يبدو سلامة وهو صبرُ صيانة. المقيس أيضًا أن الاختبارات تستورد
`../adapters/edge-rag/rag_core.js` بينما السيرفر يخدم `dashboard/public/rag_core.js` — فالجهة
المختبَرة غير الجهة المخدومة، ولا جسر. وازدواج `index.html` كان أسوأ من تكرار:
`dashboard/index.html` (13 سطرًا، مدخل Vite) مقابل `dashboard/public/index.html` (67 سطرًا، لوحة
ساكنة موازية داخل `publicDir`) — لا حارس، لا وسم، ولا قارئ. **تصحيح مقيس لاحقًا (O20)**: ليست
تصادم بناء — Vite 5 لا ينسخ `public/index.html` فوق مدخله ولا يُحذّر، والسيرفر يحلّ `/index.html` من
`dist`؛ فالضرر المقيس أنها كانت ميتة قابلة للتحرير بلا أن يراها أحد؛ ونسخة `workspace.html` في `public/` لا يخدمها السيرفر أصلًا (يخدمها من الجذر).

**الإصلاح**: `tools/sync-dashboard-assets.mjs` هو الموضع الوحيد الذي يُعرِّف الاتجاه —
`ASSET_MIRRORS` تسعة أزواج `(source → dest)` مع سبب مكتوب لكل زوج، و`--check` يفحص ولا يكتب،
و`--write` يكتب كتابةً ذرّية (tmp+rename) فلا تُخدَم مرآةٌ نصف ملف؛ وموصول في `package.json`
(`sync:assets` / `sync:assets:check`) ليكون نابض CI جاهزًا (تُستعمَل في D1.16). وحارس
`tests/duplicate-sync.test.js` يفرض: التطابق الآن؛ أن الاتجاه مُعلَّن من جهة الاختبار لا من
الأقرب؛ **ألّا توجد نسخة ظلّ غير معلنة** (مسح `adapters/ dashboard/ src/ tools/ packages/ spec/
docs/` بحدّ 32 بايتًا، وكل زوج متطابق لا يغطيه إعلانُ المرايا يكسر)؛ أن `dashboard/public/index.html`
زائل وأن `dashboard/index.html` باقٍ مدخل Vite؛ أن الأرشيف مُعلَن ومخدوم وموسوم؛ وألّا يكون الفحص
ثقةً عمياء: على جذر وهمي بمنرايين منحرفتين يجب أن يرى `drifted.length === 2` ويسمّي الملف ويخرج 1،
وأن `--check` **لا يصلح** القرص.

**حسم الازدواج لا بإلغاء**: نُقلت اللوحة الساكنة إلى `dashboard/legacy/static-dashboard.html` مع
`dashboard/legacy/README.md` (كان/صار/لماذا + اسم الحارس)، وتُخدَم عند `/dashboard-static.html`
و`/static` بترويسة `X-Nexa-Page: legacy-static (not the production build)`، وتُجيب 410 لا صمتًا إن
فُقدت. لا حذف توثيق ولا حذف صفحات: النقل موسوم — وهو نفس معيار فهرس الوثائق (O18).

**الأدلة**: حمراء-كإثبات 2 من 2 (`d1.15-red.tap`: التطابق يدوي ولا سكربت ولا حارس + التصادم غير
محسوم) ثم 6 من 6 (`d1.15-green.tap`)؛ المجموعة الكاملة **684/684/0** و`sync:assets:check` يقول
`in sync · 9 مرآة مطابقة` و`npm run metrics` أخضر و`npm run verify` exit 0. الانحراف السباعي
(`d1.15-inversion.txt`): تحرير المرآة يدويًا ⇒ 2 من 6؛ نسخة ظلّ جديدة ⇒ 1؛ عودة `public/index.html`
⇒ 2 (تصادم + ظلّ)؛ فحص بلا `exit 1` ⇒ 1؛ فاحص يتجاهل الجذر الممرر ⇒ 1؛ `--check` يصلح القرص ⇒ 1؛
و**H7 عرضًا**: بإزالة صفّ `NEXA_SYNC_ROOT` أحمرّ حارس D1.13 (5 من 6) ثم اخضرّ بإعادته — الحارس
الذي سبق التذكرة ردّ على قراءتها الجديدة، لا أنا ادّعيت ذلك. حراس edge-rag (4 و6)
وdetector-independence (7) وdocs-consistency (6) وserver-perimeter (9) خضراء في النسخ كلها.
ملحق في نفس الملف يوثّق أن H5 وH6 قِيستا مرّتين: الأولى أنتجت خطأ بناء/أثرًا خاليًا، والثانية
بعد تعزيز الاختبار السادس — الأمانة في سجل الانحراف ليست تفصيلًا، هي نصف الدليل.

**ما لم يُفعل عمدًا**: لا نقل للمرآة إلى `gitignore` وتوليد في البناء (يُغيّر ما هو متتبَّع؛ يبقى
لـ Phase 1 أو لتذكرة بناء الداشبورد)؛ ولا ماسح AST للتحليل (hash بالبايت كافٍ ومُثبَت)؛
ولا أرشفة فعلية للوحة الساكنة (تُخدَم وتُوسم)؛ ولا لمس لـ `dashboard/data/*.json` المتتبَّعة (تلوّث
شجرة من فئة أخرى، سُجّلت متابعة)؛ ولا `packages/` ولا `spec/`.

### O20. تصحيح مقيس لفقرة في O19 — «التصادم في البناء» لم يكن صحيحًا (2026-09-24)

في O19 كُتب أن `dashboard/public/index.html` كانت «تُنسَخ إلى `dist/` باسم ملف البناء نفسه». قِيست
الحالة بإرجاع الملف وإعادة البناء (Vite 5.4.21، `npm --prefix dashboard run build`): `dist/index.html`
يبقى مخرج البناء (406 بايتًا، `sha1 c8e2247b…`) في الحالتين — لا استبدال بالظلّ (`5e5ecc8a…`) ولا
تحذير من Vite. وحلّ السيرفر يخدم `/index.html` والـ SPA fallback من `dist` (`:3016`، `:3132`)، فالظلّ
لم يُخدَم من `publicDir` قطّ. الضرر الحقيقي أصغر في الأثر وأخسّ في الطبيعة: **نسخة ميتة قابلة للتحرير**
— من عدّلها بدت محدَّثة ولم يقرأها أحد، ولا حارس يقول إنها بلا قارئ. لذلك بقي حكم النقل كما هو،
وصار **السبب** في الحارس (`tests/duplicate-sync.test.js`) وفي `dashboard/legacy/README.md` وفي
`self-model/gaps.json` هو المقيس لا المستنتج من صياغة التذكرة. القياس كاملًا في
`self-model/evidence/d1.15-correction.txt`. تصحيح السجل ليس إعادة كتابة لـ O19 (ملحق)، ولا إسقاطًا
للادّعاء الخاطئ (يبؤ مذكورًا هنا) — وهو نفس معيار الأرشيف: لا حذف، وسمٌ ومسار.

### O21. سجل إغلاق D1.16 (P2 / O08) — البناء يُرى في PR، ويُفحص مخرجه لا رمز خروجه (2026-09-24)

**الحقيقة المقيسة**: الداشبورد كان يُبنى في مكان واحد فقط — `deploy-pages.yml`، ومقطع تشغيله
`on: push: branches: ["main"]` مع `workflow_dispatch`. أي أن كسر Vite/React يصل `main` أولًا ثم
يُكتشَف عند النشر. و`ci.yml` — الوحيد الذي يرى كل `pull_request` — كان لا يذكر كلمة `dashboard`
مرة واحدة (قِيس: المُعيد أخضر 1 من 1). و`metrics.yml` يعمل على PRs لكنه لا يبني الواجهة. لم يكن
الإصلاح «أضف workflow»: كان «الوظيفة موجودة في المكان الخطأ بالنسبة للـ PR».

**الإصلاح**: وظيفة `dashboard:` مستقلة في `ci.yml` (إشارة حمراء مستقلة، ولا `npm install` في وظيفة
kernel صفر-الاعتمادية): `npm ci` من `dashboard/package-lock.json` (القفل محلي بالداشبورد؛ الجذر
بلا قفل عمدًا)، ثم `npm run build`، ثم **فحص المخرج**: `dashboard/dist/index.html` موجود ويشير إلى
أصل باسمه بصمة المحتوى، ولا `dashboard/public/index.html` في `publicDir` (حكم D1.15)، و`git ls-files
-- dashboard/dist` فارغ. وفي وظيفة kernel خطوة `npm run sync:assets:check` — نابض D1.15 موصول، في
الموضع الذي لا يحتاج تثبيتًا.

**الأدلة**: حمراء 1 من 1 (`d1.16-red.tap`) ثم 6 من 6 (`d1.16-green.tap`)، وبروفة محليّة لأوامر
الوظيفة نفسها حرفيًا: `npm ci` (131 حزمة، exit 0)، `vite build` (1906 وحدة، `dist/index.html` 0.41 kB
في 2.80s)، الفحوص الثلاثة exit 0، `sync:assets:check` يقول `in sync · 9 مرآة مطابقة`.
المجموعة **689/689/0** و`metrics` أخضر و`verify` exit 0. الانحراف الثماني (`d1.16-inversion.txt`):
حذف الوظيفة ⇒ 3 من 6؛ `continue-on-error` على البناء ⇒ 1؛ `cache-dependency-path` إلى قفل لا وجود
له ⇒ 1؛ `npm install` داخل وظيفة kernel ⇒ 1؛ نزع خطوة فحص المرايا ⇒ 1؛ تغيير اسم `build` في
`dashboard/package.json` ⇒ 1؛ إلغاء استثناء `dashboard/dist` من `.gitignore` ⇒ 1.
والثامنة **R1 ليست فجوة في الإصلاح بل في الحارس**: تعليق ينتحل خطوات `npm ci`/`npm run build` كان
يكفي لإرضاء `assert.match` حتى جُعل القارئ يتخطّى التعليقات — قِيس فأحمرّ اختبارين ثم اخضرّا.
المجاورون (حارس المرايا 6، edge-rag 4، وثائق 6، سجل الفجوات 5) خضر في الثمانية، والاستعادة 27 من 27.

**الوضع بعد هذه التذكرة**: `self-model/gaps.json` لم تبقَ فيه فجوة مفتوحة — D1.3…D1.17 كلها
`enforced`. ما بعدها يحتاج إذنًا صريحًا: تفكيك `tools/celia-dashboard-server.mjs` (~3190 سطرًا)
و Phase 1، وكلها مسجَّلة `follow_ups` (تثبيت النشر بلا قفل، تكرار `npm test` في `metrics.yml`،
لا `actionlint`، توليد بيانات Hunter لا يُختبر في PRs) — تذاكر، لا إضافات إلى هذه.
